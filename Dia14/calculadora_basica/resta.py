def restar(x, y):
    """imprimir la resta de dos numeros

    Args:
        x (float): Primer numero
        y (float): Segundo numero
    """
    print(f"El resultado de la resta es : {x - y}")
    
if __name__ == "__main__":
    print("Probando el metodo resta") #lo que este bajo esta validación solo se ejecutara cuando se ejcute este archivo
    restar(4,6)